/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;

// module.exports = {
//   webpack: (config, { dev }) => {
//     // Set custom hostname only in development mode
//     if (dev) {
//       config.devServer = {
//         ...config.devServer,
//         host: 'fonts.gstatic.com',
//       };
//     }
//     return config;
//   },
// };
